# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Agent dependencies (from existing agent)
langchain>=0.1.0
langchain-core>=0.1.0
langchain-google-genai>=1.0.0
langgraph>=0.0.40
duckdb==0.10.3
pandas==2.1.4
numpy>=1.24.0,<2.0  # numpy 2.x has compatibility issues with some packages
matplotlib>=3.7.0
scipy>=1.10.0
rapidfuzz>=3.0.0

# ML dependencies
joblib>=1.3.0
scikit-learn==1.3.2
plotly>=5.0.0
seaborn>=0.12.0
tabulate>=0.9.0

# RAG dependencies (literature search with vector retrieval)
sentence-transformers>=2.2.0
qdrant-client>=1.7.0
tiktoken>=0.5.0
google-generativeai>=0.8.0  # Gemini vision for figure interpretation

# PDF parsing with table/figure extraction (requires system: poppler-utils)
unstructured[pdf]>=0.18.0
pdf2image>=1.16.0
Pillow>=10.0.0
PyPDF2>=3.0.0  # Fallback if unstructured unavailable
pdfminer.six>=20221105  # Used by unstructured fast strategy

# Optional: for better OCR (requires system: tesseract-ocr)
# pytesseract>=0.3.10

# Optional: for development
# watchfiles>=0.21.0
